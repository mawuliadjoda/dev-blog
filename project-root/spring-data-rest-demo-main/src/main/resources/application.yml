spring:

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/cqrsDB
    username: root
    password: root123
  jackson:
    property-naming-strategy: SNAKE_CASE
