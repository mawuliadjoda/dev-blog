spring:
  application:
    name: service2

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://keycloak.adjoda.com.ngrok.app/realms/adjoda-dev-realm
          jwk-set-uri: https://keycloak.adjoda.com.ngrok.app/realms/adjoda-dev-realm/protocol/openid-connect/certs
      #client:
      #  registration:
      #    keycloak:
      #      client-id: service2 # created at keycloak admin console
      #      client-secret: YqtcNpgUGx8bPvQBLJTpGVn4YeNOJHw8 # generated by keycloak
      #      authorization-grant-type: client_credentials # https://oauth.net/2/grant-types/client-credentials/
      #      scope:
      #        - service1:resource1
      #        - service2:resource2
      #      provider: keycloak
      #  provider:
      #    keycloak:
      #      issuer-uri: https://keycloak.adjoda.com.ngrok.app/realms/adjoda-dev-realm

server:
  port: 10002
